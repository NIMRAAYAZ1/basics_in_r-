library(tidyverse)
booking_df <- (GL0bk8O2Sja9G5PDtko2uQ_31e445d7ca64417eb45aeaa08ec90bf1_hotel_bookings_3_)
 # Now again using the common function 
head(booking_df)

#now lets check the summary of the function 

str(booking_df)

# now lets check the degree name 
colnames(bookings_df)

#if we want to create a new average data frame then we used the focus on average daily frames 
 booking_df <- 